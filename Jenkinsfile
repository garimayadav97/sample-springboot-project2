// Example of Jenkins pipeline script

pipeline {
    agent any
  stages {
    stage("Build") {
       steps {
          bat "mvn clean verify"
       }
   }
   // And next stages if you want to define further...
 } // End of stages
} // End of pipeline